import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex);

const state = {
    user: {}
};

const mutations = {};

const actions = {};

export default new Vuex.Store({
    actions,
    state,
    mutations
})
